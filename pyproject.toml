[project]
name = "othero"
version = "0.1.0"
description = "A minimal Python scaffold for othero"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"